# Compiler files
cache/
out/

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Local environment and generated quote journals
.env
.env.*
!.env.example
writer/quotes.jsonl

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
.worktrees/

# Keeper settlement-fraction cache (runtime state, per deployment)
keeper/settlement-cache.json

# Beta waitlist store (emails + issued invite codes — never commit)
writer/waitlist.json
.superpowers/
