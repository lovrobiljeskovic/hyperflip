# --- writer service ---
# TESTNET_RPC=            (shared with keeper)
PARLAY_VAULT_ADDRESS=
WRITER_ADDRESS=
QUOTE_SIGNER_PRIVATE_KEY=
POKER_PRIVATE_KEY=
# INFO_API_URL=https://api.hyperliquid-testnet.xyz/info
# WRITER_PORT=8787
# EDGE_BPS=500
# MIN_PREMIUM_BPS=100     (must match on-chain ParlayVault.minPremiumBps)
# MIN_LEGS=2
MAX_STAKE=
PER_MARKET_CAP=
PER_CLUSTER_CAP=
# CLUSTER_EDGE_BPS=300    (extra edge per same-cluster leg pair — correlation haircut)
# QUOTE_TTL_MS=30000
# LOCKOUT_MS=600000
# POKER_INTERVAL_MS=15000
PARLAY_DEPLOY_BLOCK=
# Registry file path, relative to the repo root. Also add INVITE_CODES (Task 3).
MARKETS_FILE=registry/markets.json
# Comma-separated valid invite codes; POST /quote without one is 403.
INVITE_CODES=
